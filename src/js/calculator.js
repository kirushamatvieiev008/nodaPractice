//  default export

// export default function sum(a, b) {
//     return a + b;
// }

// const plus = (a, b) => a + b;
// const minus = (a, b) => a - b;
// const split = (a, b) => a / b;
// const multiply = (a, b) => a * b;

// export default {plus, minus, split, multiply}



// named export

export const plus = (a, b) => a + b;
export const minus = (a, b) => a - b;
export const split = (a, b) => a / b;
export const multiply = (a, b) => a * b;

